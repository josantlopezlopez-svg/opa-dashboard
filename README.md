# Inicializa Git en tu proyecto
git init
git add .
git commit -m "Backend Node.js con MongoDB y alertas"
git branch -M main
git remote add origin https://github.com/TU_USUARIO/opa-dashboard-backend.git
git push -u origin main
# opa-dashboard
